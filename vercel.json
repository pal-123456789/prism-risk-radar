{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "comment": "cleanUrls intentionally OFF: it 308-redirects /x.html -> /x, and the service worker precaches .html paths + serves cache-first. A cached redirected response fails navigation requests (redirect mode != follow). Serving .html directly keeps precache URLs == served URLs.",
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "no-referrer" },
        { "key": "Content-Security-Policy", "value": "default-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src https://fonts.gstatic.com; img-src 'self' data:; script-src 'self'; worker-src 'self'; connect-src 'self'" }
      ]
    }
  ]
}
